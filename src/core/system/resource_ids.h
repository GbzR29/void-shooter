#pragma once

enum class TextureID {
    Player,
    Thruster,
    Enemy,
    Laser,
    Explosion,
    HealthBar,
    EnergyBar,
    Planet
};

enum class SoundID {
    LaserShot,
    Explosion,
    Hit
};

enum class MusicID {
    Background,
    Boss
};

enum class FontID {
    Main,
    Pixel
};
