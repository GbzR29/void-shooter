#pragma once

enum class TextureID {
    Player,
    Thruster,
    Enemy,
    Laser,
    Explosion,
    HealthBar,
    EnergyBar,
    Planet,
    Icon
};

enum class FontID {
    Main,
    Pixel
};

enum class SoundID {
    LaserShot,
    Explosion,
    Hit
};

enum class MusicID {
    Background,
    Boss
};

enum class SoundCategory {
    Music,
    SFX,
    UI
};


